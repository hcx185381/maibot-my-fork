# 硅基流动 API 切换详细教程

> **最后更新**: 2026-02-02
> **适用场景**: 当DeepSeek官网API不稳定/太贵时，切换到硅基流动作为备用方案

---

## 📊 价格对比：DeepSeek官网 vs 硅基流动

### DeepSeek 官网 API（当前使用）

| 模型 | 缓存命中输入 | 缓存未命中输入 | 输出 | 备注 |
|------|------------|--------------|------|------|
| **deepseek-chat**<br>(DeepSeek-V3.2) | **¥0.2/百万** | ¥2/百万 | **¥3/百万** | 有缓存机制，重复对话更便宜 |
| **deepseek-reasoner**<br>(DeepSeek-V3.2思考模式) | **¥0.2/百万** | ¥2/百万 | **¥3/百万** | 推理专用 |

**新用户福利**: 赠送¥10（约100-150万Tokens）

---

### 硅基流动 SiliconFlow

| 模型 | 输入 | 输出 | 备注 |
|------|------|------|------|
| **deepseek-ai/DeepSeek-V3.2** | **¥2/百万** | **¥8/百万** | 与官网模型相同 |
| **deepseek-ai/DeepSeek-R1** | **¥4/百万** | **¥16/百万** | 推理专用 |

**新用户福利**:
- 赠送 **¥14**（约2000万Tokens）
- 使用邀请码额外获得 **500万Tokens**
- **部分小模型永久免费**（如Qwen2.5-7B、PaddleOCR-VL）

邀请码: `lRFkP3Qc` 或 `i05xEFBt`

---

### 💡 费用计算示例

假设每月使用 **1000万Tokens**（约1000条消息）：

| 场景 | DeepSeek官网 | 硅基流动 | 差价 |
|------|------------|----------|------|
| **全部用免费额度** | ¥0（用完10元） | ¥0（用完14元+500万） | 硅基流动多4元额度 |
| **付费期（70%缓存命中）** | ¥0.2×7M + ¥2×3M = **¥2**/输入<br>¥3×2M = **¥6**/输出<br>**总计: ¥8** | ¥2×10M = **¥20**/输入<br>¥8×2M = **¥16**/输出<br>**总计: ¥36** | **官网便宜4.5倍！** |
| **付费期（无缓存）** | ¥2×10M = **¥20**/输入<br>¥3×2M = **¥6**/输出<br>**总计: ¥26** | ¥2×10M = **¥20**/输入<br>¥8×2M = **¥16**/输出<br>**总计: ¥36** | **官网便宜1.4倍** |

---

### 🎯 结论

**使用DeepSeek官网API更便宜！**

| 对比项 | DeepSeek官网 ✅ | 硅基流动 |
|--------|----------------|----------|
| **缓存命中时** | ¥0.2/百万（便宜10倍！） | ¥2/百万 |
| **缓存未命中** | ¥2/百万（相同） | ¥2/百万 |
| **输出价格** | ¥3/百万（便宜2.7倍） | ¥8/百万 |
| **新用户赠送** | ¥10（100-150万Tokens） | ¥14（2000万Tokens） |
| **稳定性** | 官方源，最稳定 | 可能限流 |
| **推荐使用** | ⭐⭐⭐⭐⭐ 首选 | ⭐⭐⭐ 备用 |

**建议策略**:
1. **日常使用**: DeepSeek官网API（有缓存，更便宜）
2. **备用方案**: 硅基流动（新用户额度大，但单价贵）
3. **长期使用**: DeepSeek官网（缓存机制让成本极低）

---

## 📖 切换教程：从 DeepSeek 官网切换到硅基流动

### 前提条件

- ✅ 已注册硅基流动账号
- ✅ 已获取硅基流动API Key
- ✅ MaiBot正在运行

---

### 步骤1：获取硅基流动API Key

1. 访问：https://cloud.siliconflow.cn
2. 注册/登录账号
3. 左侧菜单 → "API密钥"
4. 点击"新建API密钥"
5. **复制生成的密钥**（格式：`sk-xxxxx`）
6. 可选：输入邀请码 `lRFkP3Qc` 获取额外500万Tokens

---

### 步骤2：编辑配置文件

打开文件：`docker-config/mmc/model_config.toml`

#### 2.1 确认API提供商配置（第15-22行）

```toml
[[api_providers]]
name = "SiliconFlow"
base_url = "https://api.siliconflow.cn/v1"
api_key = "sk-你的API密钥"  # ← 替换成你的API Key
client_type = "openai"
max_retry = 3
timeout = 120
retry_interval = 5
```

**确保 `api_key` 是你的硅基流动API Key**

---

#### 2.2 添加硅基流动的模型定义

在 `[[models]]` 部分添加以下内容（约在第80行后）：

```toml
# ==================== 硅基流动 - 文本模型 ====================

# DeepSeek-V3.2（日常聊天）
[[models]]
model_identifier = "deepseek-ai/DeepSeek-V3.2"
name = "deepseek-ai/DeepSeek-V3.2"
api_provider = "SiliconFlow"
price_in = 2
price_out = 8
force_stream_mode = false

# DeepSeek-R1（推理专用）
[[models]]
model_identifier = "deepseek-ai/DeepSeek-R1"
name = "deepseek-ai/DeepSeek-R1"
api_provider = "SiliconFlow"
price_in = 4
price_out = 16
force_stream_mode = false

# ==================== 硅基流动 - 视觉模型 ====================

# PaddleOCR-VL（完全免费，OCR和文档解析）
[[models]]
model_identifier = "PaddlePaddle/PaddleOCR-VL"
name = "PaddlePaddle/PaddleOCR-VL"
api_provider = "SiliconFlow"
price_in = 0
price_out = 0
force_stream_mode = false

# Qwen2-VL-7B-Instruct（超便宜，通用视觉理解）
[[models]]
model_identifier = "Qwen/Qwen2-VL-7B-Instruct"
name = "Qwen/Qwen2-VL-7B-Instruct"
api_provider = "SiliconFlow"
price_in = 0.5
price_out = 2
force_stream_mode = false
```

---

#### 2.3 修改任务配置

将以下任务的 `model_list` 改为使用硅基流动的模型：

```toml
[model_task_config.utils]
model_list = ["Qwen/Qwen2.5-7B-Instruct"]  # 使用免费模型
temperature = 0.2
max_tokens = 2000
slow_threshold = 10
selection_strategy = "random"

[model_task_config.tool_use]
model_list = ["deepseek-ai/DeepSeek-R1"]  # 使用硅基流动的推理模型
temperature = 0.5
max_tokens = 2000
slow_threshold = 8
selection_strategy = "random"

[model_task_config.replyer]
model_list = ["deepseek-ai/DeepSeek-V3.2"]  # 使用硅基流动的V3
temperature = 0.3
max_tokens = 2000
slow_threshold = 15
selection_strategy = "random"

[model_task_config.planner]
model_list = ["deepseek-ai/DeepSeek-R1"]  # 使用硅基流动的推理模型
temperature = 0.3
max_tokens = 2000
slow_threshold = 10
selection_strategy = "random"

[model_task_config.vlm]
model_list = ["PaddlePaddle/PaddleOCR-VL", "Qwen/Qwen2-VL-7B-Instruct"]  # 使用免费视觉模型
max_tokens = 2000
slow_threshold = 15
selection_strategy = "random"
```

---

### 步骤3：重启容器

在项目根目录执行：

```bash
docker-compose restart core
```

等待约10-20秒，容器重启完成。

---

### 步骤4：验证配置

查看启动日志：

```bash
docker logs maim-bot-core --tail 50
```

查找关键信息：
- ✅ `非常的新鲜，非常的美味！` - 配置加载成功
- ❌ `模型名称存在重复` - 模型定义重复，需检查
- ❌ `Api key is invalid` - API Key错误，需检查

---

### 步骤5：测试对话

在QQ群里给机器人发消息测试：

1. **简单测试**: "你好"
2. **复杂测试**: "1+1等于几？"
3. **视觉测试**: 发送一张图片

如果机器人正常回复，说明切换成功！

---

## 🔧 故障排查

### 问题1：API Key无效

**错误**: `Api key is invalid`

**解决**:
1. 检查API Key是否正确复制（格式：`sk-xxxxx`）
2. 确认API Key是否已激活
3. 重新生成API Key

---

### 问题2：模型不存在

**错误**: `Model does not exist`

**解决**:
1. 检查模型名称是否正确
2. 访问 https://siliconflow.cn/models 确认可用模型
3. 常用正确名称：
   - `deepseek-ai/DeepSeek-V3.2`
   - `deepseek-ai/DeepSeek-R1`
   - `Qwen/Qwen2.5-7B-Instruct`
   - `PaddlePaddle/PaddleOCR-VL`

---

### 问题3：模型名称重复

**错误**: `模型名称存在重复`

**解决**:
1. 检查 `[[models]]` 部分
2. 确保 `model_identifier` 唯一
3. 删除重复的模型定义

---

### 问题4：机器人不回复

**检查**:
1. 查看日志：`docker logs maim-bot-core -f`
2. 确认容器运行中：`docker ps`
3. 检查网络连接和代理设置

---

## 🔄 切回DeepSeek官网

如果想切回DeepSeek官网API，只需：

1. 修改任务配置中的 `model_list`：
   ```toml
   model_list = ["deepseek-chat"]  # 或 "deepseek-reasoner"
   ```

2. 重启容器：
   ```bash
   docker-compose restart core
   ```

3. 硅基流动的配置可以保留，不影响使用

---

## 📝 快速配置模板

### 最小配置（仅测试）

如果只是想测试硅基流动，只需修改一个任务：

```toml
[model_task_config.replyer]
model_list = ["deepseek-ai/DeepSeek-V3.2"]  # 改为硅基流动
temperature = 0.3
max_tokens = 2000
slow_threshold = 15
selection_strategy = "random"
```

这样只有回复消息会使用硅基流动，其他任务仍用DeepSeek官网。

---

### 完整切换（所有任务）

按步骤2.2和2.3修改所有配置，实现完全切换。

---

## 💡 使用建议

### 何时使用硅基流动

✅ **推荐使用**:
- DeepSeek官网API故障或不稳定
- 新用户想用完14元+500万Tokens免费额度
- 需要使用硅基流动独有模型（如免费小模型）

❌ **不推荐使用**:
- 长期稳定使用（比官网贵）
- 对成本敏感的用户
- 高并发场景（硅基流动可能限流）

---

### 成本优化策略

1. **混合使用**: 回复用DeepSeek官网，工具任务用硅基流动的免费模型
2. **先用免费额度**: 优先使用硅基流动的14元额度
3. **定期检查**: 定期查看两个平台的余额和使用情况

---

## 📚 相关资源

- **硅基流动官网**: https://siliconflow.cn
- **硅基流动模型列表**: https://siliconflow.cn/models
- **硅基流动控制台**: https://cloud.siliconflow.cn
- **DeepSeek官网**: https://www.deepseek.com
- **DeepSeek API文档**: https://api-docs.deepseek.com

---

## 🎉 总结

1. **DeepSeek官网更便宜**（缓存命中时便宜10倍）
2. **硅基流动作为备用**（新用户额度大）
3. **切换很简单**（修改配置+重启容器）
4. **可以随时切回**（配置互不影响）

**现在继续使用DeepSeek官网API是明智的选择！** ✅

---

**文档版本**: v1.0
**创建时间**: 2026-02-02
**创建工具**: Claude Code
