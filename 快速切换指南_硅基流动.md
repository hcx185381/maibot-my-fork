# 🚀 快速切换指南：DeepSeek官网 ↔ 硅基流动

> **文件位置**: `docker-config/mmc/model_config.toml`
> **样本文件**: `model_config_siliconflow_sample.toml`

---

## 📋 方法1：直接替换文件（最简单）

### 步骤1：备份当前配置

```bash
cd "E:\github ai xiangmu\MaiBot"
copy docker-config\mmc\model_config.toml docker-config\mmc\model_config.toml.backup
```

### 步骤2：替换API Key

打开 `model_config_siliconflow_sample.toml`，找到第18行：

```toml
api_key = "sk-idjdrtdithcxuozmairymdebbovithfcidkvnavnchwnxavh"  # ← 替换成你的
```

替换成你自己的硅基流动API Key。

### 步骤3：覆盖配置文件

```bash
copy docker-config\mmc\model_config_siliconflow_sample.toml docker-config\mmc\model_config.toml
```

### 步骤4：重启容器

```bash
docker-compose restart core
```

✅ 完成！

---

## 📋 方法2：手动修改关键部分（推荐）

### 只需要修改3个地方

#### 修改1：API提供商配置（保持不变）

你的API Key已经配置好了，在第15-22行：

```toml
[[api_providers]]
name = "SiliconFlow"
base_url = "https://api.siliconflow.cn/v1"
api_key = "sk-idjdrtdithcxuozmairymdebbovithfcidkvnavnchwnxavh"
client_type = "openai"
max_retry = 3
timeout = 120
retry_interval = 5
```

#### 修改2：添加硅基流动模型

在 `deepseek-vl` 模型后面（第79行后）添加：

```toml
# ==================== 硅基流动模型 ====================

[[models]]
model_identifier = "deepseek-ai/DeepSeek-V3.2"
name = "deepseek-ai/DeepSeek-V3.2"
api_provider = "SiliconFlow"
price_in = 2
price_out = 8
force_stream_mode = false

[[models]]
model_identifier = "Qwen/Qwen2.5-7B-Instruct"
name = "Qwen/Qwen2.5-7B-Instruct"
api_provider = "SiliconFlow"
price_in = 0
price_out = 0
force_stream_mode = false

[[models]]
model_identifier = "PaddlePaddle/PaddleOCR-VL"
name = "PaddlePaddle/PaddleOCR-VL"
api_provider = "SiliconFlow"
price_in = 0
price_out = 0
force_stream_mode = false
```

#### 修改3：改任务配置

把所有 `model_list = ["deepseek-chat"]` 改成：

```toml
[model_task_config.utils]
model_list = ["Qwen/Qwen2.5-7B-Instruct"]  # 免费模型

[model_task_config.replyer]
model_list = ["deepseek-ai/DeepSeek-V3.2"]  # 硅基流动V3

[model_task_config.vlm]
model_list = ["PaddlePaddle/PaddleOCR-VL"]  # 免费OCR
```

#### 重启容器

```bash
docker-compose restart core
```

---

## 📊 配置对比表

### 当前配置（DeepSeek官网）

```toml
# 模型
[[models]]
model_identifier = "deepseek-chat"
api_provider = "DeepSeek"  # ← 官网

# 任务
[model_task_config.replyer]
model_list = ["deepseek-chat"]  # ← 官网模型
```

### 硅基流动配置

```toml
# 模型
[[models]]
model_identifier = "deepseek-ai/DeepSeek-V3.2"
api_provider = "SiliconFlow"  # ← 硅基流动

# 任务
[model_task_config.replyer]
model_list = ["deepseek-ai/DeepSeek-V3.2"]  # ← 硅基流动模型
```

---

## 🔍 关键差异对比

| 配置项 | DeepSeek官网 | 硅基流动 |
|--------|-------------|----------|
| **模型名称** | `deepseek-chat` | `deepseek-ai/DeepSeek-V3.2` |
| **API提供商** | `DeepSeek` | `SiliconFlow` |
| **输入价格** | ¥0.2-2/百万 | ¥2/百万 |
| **输出价格** | ¥3/百万 | ¥8/百万 |
| **免费额度** | ¥10 | ¥14+500万Tokens |

---

## ⚡ 快速切换命令

### 切换到硅基流动

```bash
# 1. 备份
copy docker-config\mmc\model_config.toml docker-config\mmc\model_config_deepseek.toml

# 2. 替换（先修改样本文件中的API Key）
copy docker-config\mmc\model_config_siliconflow_sample.toml docker-config\mmc\model_config.toml

# 3. 重启
docker-compose restart core
```

### 切换回DeepSeek官网

```bash
# 1. 恢复备份
copy docker-config\mmc\model_config_deepseek.toml docker-config\mmc\model_config.toml

# 2. 重启
docker-compose restart core
```

---

## 📝 检查清单

切换前检查：
- [ ] 已获取硅基流动API Key
- [ ] 已替换样本文件中的API Key
- [ ] 已备份当前配置

切换后检查：
- [ ] 容器重启成功
- [ ] 日志显示"非常的新鲜，非常的美味！"
- [ ] 机器人能正常回复

---

## 🆘 常见问题

**Q1: 切换后机器人不回复？**

A: 检查日志：
```bash
docker logs maim-bot-core --tail 50
```
查找错误信息，可能是：
- API Key错误 → 重新检查API Key
- 模型名称错误 → 对照样本文件检查
- 网络问题 → 检查代理设置

**Q2: 如何确认已切换到硅基流动？**

A: 查看日志中的模型调用统计：
```bash
docker logs maim-bot-core | grep "按模型分类统计"
```
应该看到 `deepseek-ai/DeepSeek-V3.2` 而不是 `deepseek-chat`

**Q3: 可以混合使用吗？**

A: 可以！部分任务用DeepSeek官网，部分用硅基流动：
```toml
[model_task_config.replyer]
model_list = ["deepseek-chat"]  # 官网

[model_task_config.utils]
model_list = ["Qwen/Qwen2.5-7B-Instruct"]  # 硅基流动免费
```

**Q4: 硅基流动的免费模型有哪些？**

A:
- `Qwen/Qwen2.5-7B-Instruct` - 文本生成（完全免费）
- `PaddlePaddle/PaddleOCR-VL` - OCR识别（完全免费）
- `Qwen/Qwen2-VL-7B-Instruct` - 视觉理解（¥0.5/百万，很便宜）

---

## 📚 相关文件

- **当前配置**: `docker-config/mmc/model_config.toml`
- **硅基流动样本**: `docker-config/mmc/model_config_siliconflow_sample.toml`
- **详细教程**: `硅基流动切换教程.md`
- **价格对比**: `硅基流动切换教程.md` 第1章

---

## 💡 推荐配置方案

### 方案1：完全硅基流动（新手友好）

- 优点：14元+500万Tokens免费额度
- 缺点：长期使用成本高
- 适用：新用户、测试、短期使用

### 方案2：混合使用（推荐）

- 回复、QA → DeepSeek官网（有缓存，便宜）
- 工具任务 → 硅基流动免费模型
- 优点：最大化免费额度，长期成本低
- 缺点：需要手动配置

### 方案3：全DeepSeek官网（最经济）

- 所有任务 → DeepSeek官网
- 优点：缓存机制，长期最便宜
- 缺点：免费额度少（只有¥10）
- 适用：长期稳定使用

---

**建议**: 继续使用当前的DeepSeek官网配置，等需要时再切换到硅基流动！✅

---

**创建时间**: 2026-02-02
**文档版本**: v1.0
